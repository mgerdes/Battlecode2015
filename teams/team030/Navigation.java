package team030;

import battlecode.common.Direction;
import battlecode.common.MapLocation;

public class Navigation {

    public static void init(MapLocation nwCorner) {

    }

    public static Direction getDirection(MapLocation currentLocation, int pointOfInterest) {
        //--Convert absolute location to relative location using nwCorner

        //--Read the channel corresponding to the relative current location
        //--Return the direction corresponding to the point of interest

        //--This method will not have to look up the POI
        return null;
    }
}
